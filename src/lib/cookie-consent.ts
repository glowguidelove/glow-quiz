/** localStorage key — keep in sync with CookieConsent */
export const COOKIE_CONSENT_STORAGE_KEY = "gq_cookie_consent";

/** Fired on `document` when the user accepts the cookie banner */
export const COOKIE_CONSENT_ACCEPT_EVENT = "gq-cookie-accepted";
